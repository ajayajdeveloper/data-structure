function logItems(a,b)
{
    for(let i =0 ; i < a ;i++)
    {
        console.log(i)
    }

    for(let k =0 ; k < b ;k++)
    {
        console.log(k)
    }
   
}

logItems(3,6)


//different terms for input 
// in this case we intial dont know what is a and b value so to denote the 0  
//we must use different term as o(a) and o(b) 
//if it is nested loop we must say 0(a) * 0(b) 